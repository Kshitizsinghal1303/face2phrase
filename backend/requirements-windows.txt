# Windows-compatible requirements for Face2Phrase
# Use this file for Windows installations to avoid DLL issues

# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
aiofiles==23.2.1

# Google Gemini AI
google-generativeai==0.3.1

# PyTorch CPU-only version for Windows compatibility
--index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu
torchaudio==2.1.0+cpu

# Whisper for speech-to-text (CPU version)
openai-whisper==20231117

# Audio/Video processing
ffmpeg-python==0.2.0
pydub==0.25.1

# Data handling
pydantic==2.5.0
python-dotenv==1.0.0

# Database (optional, for future use)
sqlalchemy==2.0.23

# Additional utilities
numpy==1.24.3
pandas==2.0.3

# PDF generation
reportlab==4.0.7

# Speech Analysis Dependencies (Windows compatible versions)
librosa==0.10.1
scipy==1.11.4
matplotlib==3.7.2
plotly==5.17.0
soundfile==0.12.1
numba==0.58.1

# Computer Vision (Windows compatible)
opencv-python==4.8.1.78
mediapipe==0.10.7
Pillow==10.0.1
imutils==0.5.4

# Machine Learning
scikit-learn==1.3.2

# Optional dependencies (will be handled gracefully if missing)
# praat-parselmouth==0.4.3  # Can be problematic on Windows
# face-recognition>=1.3.0   # Requires dlib which can be problematic
# fer>=22.5.0               # Heavy dependency, optional